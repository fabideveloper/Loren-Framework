--!strict
--[[
	AUTO-GENERATED by the Loren CLI.
	Do not edit by hand -- re-run `loren refresh` (or `loren make` / `loren inject`) to update.

	Only literal name unions live here, never `typeof(require(...))` of a Service or
	Controller -- every Service/Controller requires this file, so doing that here would
	create a require cycle for every single one of them. For a fully typed reference to
	one specific dependency, cast it inline where you consume it, e.g.:
		local Foo = self.Dependencies.Foo :: typeof(require(ServerScriptService.Server.Services.Foo))
]]

export type ServiceName = "ExampleService"
export type ControllerName = "ExampleController"

return nil
